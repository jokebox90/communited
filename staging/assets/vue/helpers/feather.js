
import * as feather from "feather-icons";
import _ from "lodash";

export const featherReplace = () => feather.replace();
// export const featherReplace = _.throttle(() => feather.replace(), 10, {
//   leading: true,
//   trailing: true,
// });